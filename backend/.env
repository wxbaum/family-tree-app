# ==================================================================
# FAMILY TREE API - GOOGLE CLOUD FIRESTORE ENVIRONMENT
# ==================================================================
# Clean configuration for Google Cloud Firestore-based application

# ==================================================================
# GOOGLE CLOUD CONFIGURATION
# ==================================================================

# Google Cloud Project ID (replace with your project ID)
GOOGLE_CLOUD_PROJECT=family-tree-app-465402

# Service Account Key Path (for local development)
# Leave empty to use Application Default Credentials (for Cloud deployment)
FIREBASE_CREDENTIALS_PATH=serviceAccountKey.json

# Firestore Collection Prefix (for testing isolation)
# Leave empty for production, use "test_" for testing
FIRESTORE_COLLECTION_PREFIX=

# Google Cloud Storage Bucket Name
GCS_BUCKET_NAME=your-project-id-files

# ==================================================================
# APPLICATION SETTINGS
# ==================================================================

# Application Metadata
PROJECT_NAME=Family Tree API
VERSION=2.0.0
DEBUG=true

# Security (still used for any custom JWT operations)
SECRET_KEY=dev-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ==================================================================
# CORS CONFIGURATION
# ==================================================================

# CORS Origins (comma-separated list)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000

# ==================================================================
# FILE UPLOAD CONFIGURATION
# ==================================================================

# File Upload Limits (10MB)
MAX_FILE_SIZE=10485760

# Allowed File Types (comma-separated list)
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,image/webp,application/pdf,text/plain,video/mp4,video/avi,video/mov,audio/mpeg,audio/wav

# ==================================================================
# LOGGING CONFIGURATION
# ==================================================================

LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# ==================================================================
# DEVELOPMENT SETTINGS
# ==================================================================

# Testing
TESTING=false
TEST_DATABASE_PREFIX=test_

# Development Features
ENABLE_API_DOCS=true
ENABLE_CORS=true

# Rate Limiting (requests per minute)
API_RATE_LIMIT_PER_MINUTE=100
FILE_UPLOAD_RATE_LIMIT_PER_MINUTE=10

# ==================================================================
# GOOGLE CLOUD PROJECT SETUP INSTRUCTIONS
# ==================================================================
# 
# To set up Google Cloud for this project:
#
# 1. Create a Google Cloud project at https://console.cloud.google.com/
# 2. Enable the Firestore API
# 3. Enable the Cloud Storage API
# 4. Create a Firestore database in Native mode
# 5. Create a service account:
#    - Go to IAM & Admin > Service Accounts
#    - Click "Create Service Account"
#    - Give it roles: "Cloud Datastore User" and "Storage Admin"
#    - Create and download a JSON key
#    - Save as `serviceAccountKey.json` in the backend directory
# 6. Update GOOGLE_CLOUD_PROJECT above with your project ID
#
# Alternative authentication methods:
# 1. Use gcloud: `gcloud auth application-default login`
# 2. Set GOOGLE_APPLICATION_CREDENTIALS environment variable
# 3. Use attached service account (for Cloud Run deployment)
#
# ==================================================================