# ==================================================================
# FAMILY TREE API - DEVELOPMENT ENVIRONMENT CONFIGURATION
# ==================================================================
# This file contains default development settings that work out of
# the box. For production, copy this file and update with secure values.

# ==================================================================
# DATABASE CONFIGURATION
# ==================================================================
# Local development database (matches docker-compose.yml)
DATABASE_URL=postgresql+asyncpg://family_tree_user:family_tree_password@localhost:5432/family_tree_db

# For Docker Compose (when running backend in container)
# DATABASE_URL=postgresql+asyncpg://family_tree_user:family_tree_password@db:5432/family_tree_db

# ==================================================================
# SECURITY CONFIGURATION
# ==================================================================
# IMPORTANT: Generate a secure secret key for production using:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=dev-secret-key-change-this-in-production-use-secrets-token-urlsafe

# JWT Configuration
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ==================================================================
# APPLICATION SETTINGS
# ==================================================================
DEBUG=true
PROJECT_NAME=Family Tree API
VERSION=1.0.0

# CORS Origins (comma-separated list)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000

# ==================================================================
# FILE STORAGE CONFIGURATION
# ==================================================================
# File upload limits
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,image/webp,application/pdf

# Local development file storage
FILE_STORAGE_TYPE=local
UPLOAD_PATH=uploads

# ==================================================================
# ORACLE OBJECT STORAGE (OPTIONAL - FOR PRODUCTION)
# ==================================================================
# Leave empty for local development, configure for production deployment
ORACLE_NAMESPACE=
ORACLE_BUCKET_NAME=family-tree-files
ORACLE_ACCESS_KEY=
ORACLE_SECRET_KEY=
ORACLE_REGION=us-ashburn-1

# ==================================================================
# DEVELOPMENT NOTES
# ==================================================================
# 1. This configuration works with the included docker-compose.yml
# 2. The database will be created automatically when using Docker
# 3. For local development without Docker, ensure PostgreSQL is running
# 4. Run database migrations with: alembic upgrade head
# 5. Start the server with: uvicorn app.main:app --reload