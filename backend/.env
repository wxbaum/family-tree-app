# ==================================================================
# FAMILY TREE API - FIRESTORE DEVELOPMENT ENVIRONMENT
# ==================================================================
# Clean configuration for Firebase/Firestore-based application
# This file contains default development settings that work out of the box.

# ==================================================================
# GOOGLE CLOUD / FIREBASE CONFIGURATION
# ==================================================================

# Google Cloud Project ID (replace with your project ID)
GOOGLE_CLOUD_PROJECT=family-tree-app-465402

# Firebase Service Account Key (for local development)
# Leave empty to use Application Default Credentials (for Cloud Run)
FIREBASE_CREDENTIALS_PATH=serviceAccountKey.json

# Firestore Collection Prefix (for testing isolation)
# Leave empty for production, use "test_" for testing
FIRESTORE_COLLECTION_PREFIX=

# Google Cloud Storage Bucket Name
GCS_BUCKET_NAME=family-tree-app-files

# ==================================================================
# APPLICATION SETTINGS
# ==================================================================

# Application Metadata
PROJECT_NAME=Family Tree API
VERSION=2.0.0
DEBUG=true

# Security (still used for any custom JWT operations)
SECRET_KEY=dev-secret-key-change-this-in-production-use-firebase-auth-instead
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ==================================================================
# CORS CONFIGURATION
# ==================================================================

# CORS Origins (comma-separated list)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000

# ==================================================================
# FILE UPLOAD CONFIGURATION
# ==================================================================

# File Upload Limits (10MB)
MAX_FILE_SIZE=10485760

# Allowed File Types (comma-separated list)
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,image/webp,application/pdf,text/plain,video/mp4,video/avi,video/mov,audio/mpeg,audio/wav

# ==================================================================
# LOGGING CONFIGURATION
# ==================================================================

LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# ==================================================================
# DEVELOPMENT SETTINGS
# ==================================================================

# Testing
TESTING=false
TEST_DATABASE_PREFIX=test_

# Development Features
ENABLE_API_DOCS=true
ENABLE_CORS=true

# Rate Limiting (requests per minute)
API_RATE_LIMIT_PER_MINUTE=100
FILE_UPLOAD_RATE_LIMIT_PER_MINUTE=10

# ==================================================================
# FIREBASE PROJECT SETUP INSTRUCTIONS
# ==================================================================
# 
# To set up Firebase for this project:
#
# 1. Create a Firebase project at https://console.firebase.google.com/
# 2. Enable Firestore Database in Native mode
# 3. Enable Authentication with Email/Password provider
# 4. Enable Cloud Storage
# 5. Generate a service account key:
#    - Go to Project Settings > Service Accounts
#    - Click "Generate new private key"
#    - Save as `serviceAccountKey.json` in the backend directory
# 6. Update GOOGLE_CLOUD_PROJECT above with your project ID
#
# For Cloud Run deployment:
# 1. Set FIREBASE_CREDENTIALS_PATH to empty string
# 2. Ensure Cloud Run service account has Firestore permissions
# 3. Update GOOGLE_CLOUD_PROJECT with your production project ID
#
# ==================================================================